🚛 Система управління транспортною логістикою

Logistics Microservices

Мікросервісна система для управління доставками, транспортом і білінгом. Реалізована на Java 17 з використанням Quarkus та гібридної міжсервісної комунікації (REST + gRPC).

⸻

📋 Опис проєкту

Система побудована за мікросервісною архітектурою з чітким розподілом відповідальностей між сервісами. Такий підхід забезпечує масштабованість, ізоляцію помилок і гнучкість розвитку.

Основні можливості
•	Прийом і маршрутизація запитів через API Gateway
•	Управління життєвим циклом відправлень
•	Синхронна резервація транспорту через gRPC
•	Автоматичне формування рахунків

⸻

🛠️ Технологічний стек
•	Мова: Java 17
•	Фреймворк: Quarkus (latest stable)
•	Збірка: Maven 3.8+
•	Комунікація:
•	REST (JSON): зовнішні клієнти та некритичні міжсервісні виклики
•	gRPC (Protobuf): високопродуктивна взаємодія між сервісами
•	API документація: OpenAPI / Swagger UI

⸻

🏗️ Архітектура

Система реалізує патерн API Gateway.

graph TD
Client((Client)) -->|REST / HTTP| Gateway[API Gateway :8080]

    subgraph Internal Network
        Gateway -->|REST| Shipments[Shipments Service :8081]
        Gateway -->|REST| Fleet[Fleet Service :8082]
        Gateway -->|REST| Billing[Billing Service :8083]

        Shipments -->|gRPC| Fleet
        Billing -->|REST| Shipments
    end

Компоненти системи

Сервіс	Порт	Тип	Опис
API Gateway	8080	REST	Єдина точка входу, маршрутизація запитів
Shipments	8081	REST / gRPC Client	Управління відправленнями, ініціація gRPC
Fleet	8082	REST / gRPC Server	Управління транспортом і водіями
Billing	8083	REST	Формування рахунків і фінансові операції


⸻

🚀 Швидкий старт

Вимоги

Перед запуском переконайтеся, що встановлено:
•	Java 17 (java -version)
•	Maven (mvn -version)

Локальний запуск (Dev Mode)

Кожен сервіс запускається окремо. Quarkus підтримує Live Coding, перезапуск при зміні коду не потрібен.

Fleet Service (gRPC Server):

cd fleet-service
./mvnw quarkus:dev

Shipments Service:

cd shipments-service
./mvnw quarkus:dev

Billing Service:

cd billing-service
./mvnw quarkus:dev

API Gateway:

cd api-gateway
./mvnw quarkus:dev

Примітка: Fleet Service бажано запускати одним із перших, оскільки Shipments Service встановлює gRPC-зʼєднання під час старту.

⸻

📖 API документація (Swagger UI)

Swagger UI доступний після запуску сервісів:
•	Gateway: http://localhost:8080/q/swagger-ui
•	Shipments: http://localhost:8081/q/swagger-ui
•	Fleet: http://localhost:8082/q/swagger-ui
•	Billing: http://localhost:8083/q/swagger-ui

⸻

🧪 Тестування gRPC

gRPC-взаємодія використовується між Shipments та Fleet під час створення відправлення для перевірки доступності транспорту.

.proto файли розміщені в каталозі:

src/main/proto/

у відповідних сервісах.