# 🚛 Система управління транспортною логістикою (Logistics Microservices)

Мікросервісна архітектура для управління доставками, транспортом та білінгом. Проєкт реалізовано на **Java 17** та фреймворку **Quarkus** ("Supersonic Subatomic Java") з використанням гібридної комунікації (REST + gRPC).

## 📋 Опис проєкту

Система вирішує задачі логістики, розподіляючи відповідальність між чотирма незалежними сервісами. Це дозволяє масштабувати окремі компоненти та забезпечує відмовостійкість.

### Основні можливості:
* Прийом замовлень через єдину точку входу (Gateway).
* Управління життєвим циклом відправлень.
* Синхронна резервація транспорту через швидкий протокол gRPC.
* Автоматичне формування рахунків.

## 🛠️ Технологічний стек

* **Мова:** Java 17+
* **Фреймворк:** Quarkus (Latest stable)
* **Збірка:** Maven 3.8+
* **Комунікація:**
    * REST (JSON) — для зовнішніх клієнтів та некритичних міжісервісних запитів.
    * gRPC (Protobuf) — для високошвидкодіючої взаємодії між сервісами (Shipments -> Fleet).
* **API Doc:** OpenAPI / Swagger UI.

## 🏗️ Архітектура

Система побудована за патерном **API Gateway**.

```mermaid
graph TD
    User((Client)) -->|REST / HTTP| Gateway[API Gateway :8080]
    
    subgraph Internal Network
        Gateway -->|REST| Shipments[Shipments Service :8081]
        Gateway -->|REST| Fleet[Fleet Service :8082]
        Gateway -->|REST| Billing[Billing Service :8083]
        
        Shipments -->|gRPC| Fleet
        Billing -->|REST| Shipments
    end

Компоненти системи

Сервіс,Порт,Тип,Опис
API Gateway,8080,REST,Точка входу. Маршрутизація запитів до внутрішніх сервісів.
Shipments,8081,REST / gRPC Client,Управління замовленнями/відправленнями. Ініціює gRPC запити.
Fleet,8082,REST / gRPC Server,Управління водіями та транспортом. Обробляє gRPC запити.
Billing,8083,REST,Формування рахунків та фінансові операції.

🚀 Швидкий старт
Вимоги

Перед запуском переконайтеся, що встановлено:

    Java 17 (java -version)

    Maven (mvn -version)

Локальний запуск (Dev Mode)

Для запуску системи необхідно відкрити 4 термінали та виконати команди для кожного сервісу окремо. Quarkus підтримує "Live Coding", тому перезавантаження при змінах коду не потрібне.

Термінал 1 (Fleet Service - gRPC Server):
Bash

cd fleet-service
./mvnw quarkus:dev

Термінал 2 (Shipments Service):
Bash

cd shipments-service
./mvnw quarkus:dev

Термінал 3 (Billing Service):
Bash

cd billing-service
./mvnw quarkus:dev

Термінал 4 (API Gateway):
Bash

cd api-gateway
./mvnw quarkus:dev

    Примітка: Важливо запускати Fleet Service одним із перших, оскільки Shipments Service може намагатися встановити gRPC з'єднання під час старту.

📖 Документація API (Swagger UI)

Оскільки використовується Quarkus, документація OpenAPI генерується автоматично. Після запуску сервісів, Swagger UI доступний за адресами:

    Gateway: http://localhost:8080/q/swagger-ui

    Shipments: http://localhost:8081/q/swagger-ui

    Fleet: http://localhost:8082/q/swagger-ui

    Billing: http://localhost:8083/q/swagger-ui

🧪 Тестування gRPC

Для перевірки gRPC взаємодії між Shipments та Fleet можна викликати ендпоінт створення відправлення, який вимагає перевірки наявності автомобіля.

Приклад .proto файлу знаходиться в: src/main/proto/ (у відповідних модулях).
